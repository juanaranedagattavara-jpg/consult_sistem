---
phase: 02-professionals-services-calendar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/components/ui/select.tsx
  - src/components/ui/popover.tsx
  - src/components/ui/command.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/switch.tsx
  - src/components/ui/checkbox.tsx
  - src/components/ui/table.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/accordion.tsx
autonomous: true

must_haves:
  truths:
    - "New dependencies installed and usable"
    - "All shadcn components available for import"
  artifacts:
    - path: "package.json"
      provides: "hono, convex-helpers, @dnd-kit packages"
      contains: "hono"
    - path: "src/components/ui/table.tsx"
      provides: "Table component"
      exports: ["Table", "TableHeader", "TableBody", "TableRow", "TableCell"]
  key_links: []
---

<objective>
Install all Phase 2 dependencies and shadcn/ui components.

Purpose: Foundation for all subsequent Phase 2 work - backend HTTP API needs Hono, UI needs new shadcn components.
Output: Package.json updated, all UI components installed and ready for import.
</objective>

<execution_context>
@C:\Users\juana\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\juana\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-professionals-services-calendar/CONTEXT.md
@.planning/phases/02-professionals-services-calendar/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install npm dependencies</name>
  <files>package.json</files>
  <action>
Run npm install for all Phase 2 dependencies:

```bash
npm install hono convex-helpers @dnd-kit/core @dnd-kit/sortable
```

This installs:
- hono: HTTP routing for Convex actions (n8n API)
- convex-helpers: HonoWithConvex integration
- @dnd-kit/core: Drag-and-drop foundation
- @dnd-kit/sortable: Sortable lists for service ordering

Do NOT install cmdk separately - it comes with shadcn command component.
  </action>
  <verify>Check package.json contains all 4 dependencies: `cat package.json | grep -E "hono|convex-helpers|@dnd-kit"`</verify>
  <done>All 4 packages appear in package.json dependencies section</done>
</task>

<task type="auto">
  <name>Task 2: Install shadcn/ui components</name>
  <files>
    src/components/ui/select.tsx
    src/components/ui/popover.tsx
    src/components/ui/command.tsx
    src/components/ui/badge.tsx
    src/components/ui/separator.tsx
    src/components/ui/switch.tsx
    src/components/ui/checkbox.tsx
    src/components/ui/table.tsx
    src/components/ui/dialog.tsx
    src/components/ui/accordion.tsx
  </files>
  <action>
Install all required shadcn components for Phase 2:

```bash
npx shadcn@latest add select popover command badge separator switch checkbox table dialog accordion --yes
```

Note: Use --yes flag to skip prompts. The command component includes cmdk as a dependency.

After installation, verify each component file exists in src/components/ui/.
  </action>
  <verify>
All component files exist:
```bash
ls src/components/ui/ | grep -E "select|popover|command|badge|separator|switch|checkbox|table|dialog|accordion"
```
  </verify>
  <done>All 10 shadcn components installed in src/components/ui/</done>
</task>

</tasks>

<verification>
1. `npm ls hono convex-helpers @dnd-kit/core @dnd-kit/sortable` shows all packages installed
2. All 10 shadcn component files exist in src/components/ui/
3. No TypeScript errors: `npx tsc --noEmit`
</verification>

<success_criteria>
- Package.json contains hono, convex-helpers, @dnd-kit/core, @dnd-kit/sortable
- 10 new shadcn component files exist in src/components/ui/
- Project builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-professionals-services-calendar/02-01-SUMMARY.md`
</output>
