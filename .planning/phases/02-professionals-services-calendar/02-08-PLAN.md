---
phase: 02-professionals-services-calendar
plan: 08
type: execute
wave: 3
depends_on: ["02-01"]
files_modified:
  - src/components/ui/multi-select.tsx
autonomous: true

must_haves:
  truths:
    - "Multi-select component renders with options"
    - "Can select and deselect multiple items"
    - "Shows selected items as badges"
    - "Has search/filter functionality"
  artifacts:
    - path: "src/components/ui/multi-select.tsx"
      provides: "Multi-select component"
      exports: ["MultiSelect"]
      min_lines: 100
  key_links:
    - from: "src/components/ui/multi-select.tsx"
      to: "src/components/ui/command.tsx"
      via: "import Command"
      pattern: "import.*Command"
---

<objective>
Create multi-select component for service assignment to professionals.

Purpose: Professionals form needs multi-select for serviceIds - select multiple services they can perform.
Output: Reusable MultiSelect component with search, badges, and popover UI.
</objective>

<execution_context>
@C:\Users\juana\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\juana\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-professionals-services-calendar/02-RESEARCH.md
@src/components/ui/command.tsx
@src/components/ui/popover.tsx
@src/components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create multi-select component</name>
  <files>src/components/ui/multi-select.tsx</files>
  <action>
Create src/components/ui/multi-select.tsx based on shadcn-multi-select-component pattern:

```tsx
"use client";

import * as React from "react";
import { X, Check, ChevronsUpDown } from "lucide-react";

import { cn } from "@/lib/utils";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import {
  Command,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
} from "@/components/ui/command";
import {
  Popover,
  PopoverContent,
  PopoverTrigger,
} from "@/components/ui/popover";

export interface MultiSelectOption {
  value: string;
  label: string;
}

interface MultiSelectProps {
  options: MultiSelectOption[];
  selected: string[];
  onChange: (selected: string[]) => void;
  placeholder?: string;
  emptyMessage?: string;
  className?: string;
  maxCount?: number; // Max badges to show before "+X more"
}

export function MultiSelect({
  options,
  selected,
  onChange,
  placeholder = "Seleccionar...",
  emptyMessage = "No se encontraron opciones.",
  className,
  maxCount = 3,
}: MultiSelectProps) {
  const [open, setOpen] = React.useState(false);

  const handleSelect = (value: string) => {
    if (selected.includes(value)) {
      onChange(selected.filter((v) => v !== value));
    } else {
      onChange([...selected, value]);
    }
  };

  const handleRemove = (value: string, e: React.MouseEvent) => {
    e.stopPropagation();
    onChange(selected.filter((v) => v !== value));
  };

  const selectedLabels = selected
    .map((v) => options.find((o) => o.value === v)?.label)
    .filter(Boolean) as string[];

  const displayBadges = selectedLabels.slice(0, maxCount);
  const moreCount = selectedLabels.length - maxCount;

  return (
    <Popover open={open} onOpenChange={setOpen}>
      <PopoverTrigger asChild>
        <Button
          variant="outline"
          role="combobox"
          aria-expanded={open}
          className={cn(
            "w-full justify-between min-h-10 h-auto",
            selected.length > 0 ? "px-2 py-1" : "",
            className
          )}
        >
          <div className="flex flex-wrap gap-1">
            {selected.length === 0 && (
              <span className="text-muted-foreground">{placeholder}</span>
            )}
            {displayBadges.map((label, index) => {
              const value = selected[index];
              return (
                <Badge
                  key={value}
                  variant="secondary"
                  className="mr-1 mb-1"
                >
                  {label}
                  <button
                    className="ml-1 ring-offset-background rounded-full outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2"
                    onKeyDown={(e) => {
                      if (e.key === "Enter") {
                        handleRemove(value, e as unknown as React.MouseEvent);
                      }
                    }}
                    onMouseDown={(e) => {
                      e.preventDefault();
                      e.stopPropagation();
                    }}
                    onClick={(e) => handleRemove(value, e)}
                  >
                    <X className="h-3 w-3 text-muted-foreground hover:text-foreground" />
                  </button>
                </Badge>
              );
            })}
            {moreCount > 0 && (
              <Badge variant="secondary" className="mr-1 mb-1">
                +{moreCount} mas
              </Badge>
            )}
          </div>
          <ChevronsUpDown className="h-4 w-4 shrink-0 opacity-50" />
        </Button>
      </PopoverTrigger>
      <PopoverContent className="w-full p-0" align="start">
        <Command>
          <CommandInput placeholder="Buscar..." />
          <CommandList>
            <CommandEmpty>{emptyMessage}</CommandEmpty>
            <CommandGroup>
              {options.map((option) => (
                <CommandItem
                  key={option.value}
                  value={option.label}
                  onSelect={() => handleSelect(option.value)}
                >
                  <div
                    className={cn(
                      "mr-2 flex h-4 w-4 items-center justify-center rounded-sm border border-primary",
                      selected.includes(option.value)
                        ? "bg-primary text-primary-foreground"
                        : "opacity-50 [&_svg]:invisible"
                    )}
                  >
                    <Check className="h-4 w-4" />
                  </div>
                  {option.label}
                </CommandItem>
              ))}
            </CommandGroup>
          </CommandList>
        </Command>
      </PopoverContent>
    </Popover>
  );
}
```

Key features:
- Uses Command component for search/filter
- Shows selected items as Badge components
- maxCount limits visible badges ("+X mas" for overflow)
- Keyboard accessible
- Controlled component (selected/onChange props)
- Theme-aware colors (muted-foreground, etc.)
  </action>
  <verify>
No TypeScript errors:
```bash
npx tsc --noEmit
```
  </verify>
  <done>MultiSelect component exports and is ready for use in forms</done>
</task>

</tasks>

<verification>
1. `cat src/components/ui/multi-select.tsx` shows component with Command usage
2. `npx tsc --noEmit` succeeds
3. Component uses Badge, Command, Popover from shadcn
</verification>

<success_criteria>
- MultiSelect accepts options, selected, onChange props
- Shows badges for selected items
- Has search functionality via Command component
- Handles selection/deselection correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-professionals-services-calendar/02-08-SUMMARY.md`
</output>
