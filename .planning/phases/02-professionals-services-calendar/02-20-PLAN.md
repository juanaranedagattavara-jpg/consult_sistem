---
phase: 02-professionals-services-calendar
plan: 20
type: execute
wave: 6
depends_on: ["02-06"]
files_modified:
  - src/components/professionals/professional-table.tsx
autonomous: true

must_haves:
  truths:
    - "Table shows all professional fields"
    - "Can toggle active/inactive from table"
    - "Can select multiple rows for bulk actions"
    - "Edit and delete actions available"
  artifacts:
    - path: "src/components/professionals/professional-table.tsx"
      provides: "Table view of professionals"
      exports: ["ProfessionalTable"]
  key_links:
    - from: "src/components/professionals/professional-table.tsx"
      to: "convex/professionals.ts"
      via: "useQuery and useMutation"
      pattern: "professionals.list"
---

<objective>
Create professionals table component with selection and bulk actions.

Purpose: Admin views professionals in table format with columns for all key info, inline toggle, and bulk operations.
Output: ProfessionalTable component with selection, toggle, and action menu.
</objective>

<execution_context>
@C:\Users\juana\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\juana\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-professionals-services-calendar/CONTEXT.md
@convex/professionals.ts
@src/components/ui/table.tsx
@src/components/ui/checkbox.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create professional table component</name>
  <files>src/components/professionals/professional-table.tsx</files>
  <action>
Create src/components/professionals/professional-table.tsx:

```tsx
"use client";

import { useState } from "react";
import { useQuery, useMutation } from "convex/react";
import { api } from "@/convex/_generated/api";
import { Id } from "@/convex/_generated/dataModel";
import { toast } from "sonner";
import { Pencil, Trash2, MoreVertical, Calendar, CheckCircle, XCircle } from "lucide-react";

import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Switch } from "@/components/ui/switch";
import { Checkbox } from "@/components/ui/checkbox";
import {
  Table,
  TableBody,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "@/components/ui/table";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
} from "@/components/ui/alert-dialog";

interface ProfessionalTableProps {
  clinicId: Id<"clinics">;
  onEdit: (professionalId: Id<"professionals">) => void;
  filter?: "all" | "active" | "inactive";
}

export function ProfessionalTable({
  clinicId,
  onEdit,
  filter = "all",
}: ProfessionalTableProps) {
  const professionals = useQuery(api.professionals.listWithServices, { clinicId });
  const toggleActive = useMutation(api.professionals.toggleActive);
  const bulkToggle = useMutation(api.professionals.bulkToggleActive);
  const removeProfessional = useMutation(api.professionals.remove);

  const [selected, setSelected] = useState<Set<Id<"professionals">>>(new Set());
  const [deletingId, setDeletingId] = useState<Id<"professionals"> | null>(null);

  // Filter professionals
  const filtered = professionals?.filter((p) => {
    if (filter === "active") return p.active;
    if (filter === "inactive") return !p.active;
    return true;
  }) ?? [];

  const allSelected = filtered.length > 0 && selected.size === filtered.length;
  const someSelected = selected.size > 0 && selected.size < filtered.length;

  function toggleSelectAll() {
    if (allSelected) {
      setSelected(new Set());
    } else {
      setSelected(new Set(filtered.map((p) => p._id)));
    }
  }

  function toggleSelect(id: Id<"professionals">) {
    const newSelected = new Set(selected);
    if (newSelected.has(id)) {
      newSelected.delete(id);
    } else {
      newSelected.add(id);
    }
    setSelected(newSelected);
  }

  async function handleToggle(professionalId: Id<"professionals">) {
    try {
      await toggleActive({ professionalId });
    } catch (error) {
      toast.error("Error al cambiar estado");
    }
  }

  async function handleBulkToggle(active: boolean) {
    if (selected.size === 0) return;
    try {
      await bulkToggle({
        professionalIds: Array.from(selected),
        active,
      });
      toast.success(`${selected.size} profesionales ${active ? "activados" : "desactivados"}`);
      setSelected(new Set());
    } catch (error) {
      toast.error("Error al cambiar estados");
    }
  }

  async function handleDelete() {
    if (!deletingId) return;
    try {
      await removeProfessional({ professionalId: deletingId });
      toast.success("Profesional eliminado");
      setDeletingId(null);
      selected.delete(deletingId);
      setSelected(new Set(selected));
    } catch (error) {
      toast.error("Error al eliminar");
    }
  }

  if (!professionals) {
    return <div className="text-center py-8 text-muted-foreground">Cargando...</div>;
  }

  if (filtered.length === 0) {
    return (
      <div className="text-center py-12 text-muted-foreground">
        <p>No hay profesionales</p>
        <p className="text-sm mt-1">
          {filter !== "all" ? "Prueba cambiando el filtro" : "Crea un profesional para empezar"}
        </p>
      </div>
    );
  }

  return (
    <>
      {/* Bulk Actions Bar */}
      {selected.size > 0 && (
        <div className="flex items-center gap-4 p-3 mb-4 rounded-lg bg-muted">
          <span className="text-sm font-medium">{selected.size} seleccionados</span>
          <Button size="sm" variant="outline" onClick={() => handleBulkToggle(true)}>
            <CheckCircle className="h-4 w-4 mr-1" />
            Activar
          </Button>
          <Button size="sm" variant="outline" onClick={() => handleBulkToggle(false)}>
            <XCircle className="h-4 w-4 mr-1" />
            Desactivar
          </Button>
          <Button size="sm" variant="ghost" onClick={() => setSelected(new Set())}>
            Cancelar
          </Button>
        </div>
      )}

      <div className="border rounded-lg">
        <Table>
          <TableHeader>
            <TableRow>
              <TableHead className="w-12">
                <Checkbox
                  checked={allSelected}
                  ref={(el) => {
                    if (el) (el as HTMLButtonElement).dataset.indeterminate = someSelected ? "true" : "false";
                  }}
                  onCheckedChange={toggleSelectAll}
                />
              </TableHead>
              <TableHead>Nombre</TableHead>
              <TableHead>Email</TableHead>
              <TableHead>Especialidad</TableHead>
              <TableHead>Servicios</TableHead>
              <TableHead>Calendar</TableHead>
              <TableHead>Estado</TableHead>
              <TableHead className="w-12"></TableHead>
            </TableRow>
          </TableHeader>
          <TableBody>
            {filtered.map((professional) => (
              <TableRow key={professional._id}>
                <TableCell>
                  <Checkbox
                    checked={selected.has(professional._id)}
                    onCheckedChange={() => toggleSelect(professional._id)}
                  />
                </TableCell>
                <TableCell>
                  <div className="flex items-center gap-2">
                    <div
                      className="w-3 h-3 rounded-full"
                      style={{ backgroundColor: professional.color }}
                    />
                    <span className="font-medium">{professional.name}</span>
                  </div>
                </TableCell>
                <TableCell className="text-muted-foreground">
                  {professional.email}
                </TableCell>
                <TableCell>{professional.specialty}</TableCell>
                <TableCell>
                  <div className="flex flex-wrap gap-1">
                    {professional.services.slice(0, 2).map((s) => (
                      <Badge key={s?._id} variant="secondary" className="text-xs">
                        {s?.name}
                      </Badge>
                    ))}
                    {professional.services.length > 2 && (
                      <Badge variant="secondary" className="text-xs">
                        +{professional.services.length - 2}
                      </Badge>
                    )}
                  </div>
                </TableCell>
                <TableCell>
                  {professional.calendarId ? (
                    <Badge variant="outline" className="text-green-600 border-green-600">
                      <Calendar className="h-3 w-3 mr-1" />
                      Conectado
                    </Badge>
                  ) : (
                    <Badge variant="outline" className="text-muted-foreground">
                      Sin conectar
                    </Badge>
                  )}
                </TableCell>
                <TableCell>
                  <Switch
                    checked={professional.active}
                    onCheckedChange={() => handleToggle(professional._id)}
                  />
                </TableCell>
                <TableCell>
                  <DropdownMenu>
                    <DropdownMenuTrigger asChild>
                      <Button variant="ghost" size="icon">
                        <MoreVertical className="h-4 w-4" />
                      </Button>
                    </DropdownMenuTrigger>
                    <DropdownMenuContent align="end">
                      <DropdownMenuItem onClick={() => onEdit(professional._id)}>
                        <Pencil className="h-4 w-4 mr-2" />
                        Editar
                      </DropdownMenuItem>
                      <DropdownMenuItem
                        onClick={() => setDeletingId(professional._id)}
                        className="text-destructive"
                      >
                        <Trash2 className="h-4 w-4 mr-2" />
                        Eliminar
                      </DropdownMenuItem>
                    </DropdownMenuContent>
                  </DropdownMenu>
                </TableCell>
              </TableRow>
            ))}
          </TableBody>
        </Table>
      </div>

      {/* Delete Confirmation */}
      <AlertDialog
        open={!!deletingId}
        onOpenChange={(open) => !open && setDeletingId(null)}
      >
        <AlertDialogContent>
          <AlertDialogHeader>
            <AlertDialogTitle>Eliminar profesional</AlertDialogTitle>
            <AlertDialogDescription>
              ¿Estas seguro de eliminar este profesional? Esta accion eliminara
              tambien sus excepciones (vacaciones) pero no afectara citas existentes.
            </AlertDialogDescription>
          </AlertDialogHeader>
          <AlertDialogFooter>
            <AlertDialogCancel>Cancelar</AlertDialogCancel>
            <AlertDialogAction
              onClick={handleDelete}
              className="bg-destructive text-destructive-foreground hover:bg-destructive/90"
            >
              Eliminar
            </AlertDialogAction>
          </AlertDialogFooter>
        </AlertDialogContent>
      </AlertDialog>
    </>
  );
}
```
  </action>
  <verify>No TypeScript errors: `npx tsc --noEmit`</verify>
  <done>ProfessionalTable with selection, toggle, and bulk actions</done>
</task>

</tasks>

<verification>
1. `cat src/components/professionals/professional-table.tsx` shows component
2. `npx tsc --noEmit` succeeds
3. Table uses shadcn Table, Checkbox, Switch components
</verification>

<success_criteria>
- Table shows name (with color), email, specialty, services, calendar status, active toggle
- Checkbox selection for bulk actions
- Bulk activate/deactivate selected rows
- Edit and delete from dropdown menu
- Delete confirmation dialog
- Empty state when no professionals
</success_criteria>

<output>
After completion, create `.planning/phases/02-professionals-services-calendar/02-20-SUMMARY.md`
</output>
