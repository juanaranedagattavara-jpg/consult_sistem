---
phase: 01-foundation-auth-onboarding
plan: 33
type: execute
wave: 15
depends_on: ["01-29"]
files_modified: [src/components/dashboard/upcoming-appointments.tsx]
autonomous: true
must_haves:
  truths:
    - "Placeholder shows for appointments"
  artifacts:
    - path: "src/components/dashboard/upcoming-appointments.tsx"
      min_lines: 30
---

<objective>
Create upcoming appointments component placeholder.

Purpose: Show today's appointments on dashboard.
Output: Placeholder ready for Phase 3 integration.
</objective>

<tasks>

<task type="auto">
  <name>Create upcoming appointments</name>
  <files>src/components/dashboard/upcoming-appointments.tsx</files>
  <action>
  Create src/components/dashboard/upcoming-appointments.tsx:

  ```typescript
  import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
  import { Calendar } from "lucide-react";

  export function UpcomingAppointments() {
    // TODO: In Phase 3, this will query actual appointments
    const appointments: any[] = [];

    return (
      <Card>
        <CardHeader>
          <CardTitle className="flex items-center gap-2">
            <Calendar className="h-5 w-5" />
            Próximas citas
          </CardTitle>
        </CardHeader>
        <CardContent>
          {appointments.length === 0 ? (
            <div className="text-center py-8 text-gray-500">
              <Calendar className="h-12 w-12 mx-auto mb-3 text-gray-300" />
              <p>No hay citas programadas para hoy</p>
              <p className="text-sm mt-1">
                Las citas aparecerán aquí cuando se creen
              </p>
            </div>
          ) : (
            <div className="space-y-3">
              {appointments.map((apt, index) => (
                <div
                  key={index}
                  className="flex items-center justify-between p-3 rounded-lg border"
                >
                  <div>
                    <p className="font-medium">{apt.patientName}</p>
                    <p className="text-sm text-gray-500">{apt.serviceName}</p>
                  </div>
                  <div className="text-right">
                    <p className="font-medium">{apt.time}</p>
                    <p className="text-sm text-gray-500">{apt.professional}</p>
                  </div>
                </div>
              ))}
            </div>
          )}
        </CardContent>
      </Card>
    );
  }
  ```
  </action>
  <verify>Component renders placeholder</verify>
  <done>Upcoming appointments placeholder ready</done>
</task>

</tasks>

<verification>
- [ ] Placeholder shows when empty
- [ ] Structure ready for real data
</verification>

<success_criteria>
- Placeholder component working
- Ready for Phase 3 integration
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-onboarding/01-33-SUMMARY.md`
</output>
