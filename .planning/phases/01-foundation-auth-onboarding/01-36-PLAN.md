---
phase: 01-foundation-auth-onboarding
plan: 36
type: execute
wave: 16
depends_on: ["01-34"]
files_modified: [src/app/(dashboard)/agenda/page.tsx, src/app/(dashboard)/pacientes/page.tsx, src/app/(dashboard)/lista-espera/page.tsx]
autonomous: true
must_haves:
  truths:
    - "Placeholder pages exist for Phase 2-3 features"
  artifacts:
    - path: "src/app/(dashboard)/agenda/page.tsx"
      min_lines: 15
    - path: "src/app/(dashboard)/pacientes/page.tsx"
      min_lines: 15
    - path: "src/app/(dashboard)/lista-espera/page.tsx"
      min_lines: 15
---

<objective>
Create placeholder pages for features coming in Phase 2-3.

Purpose: Complete navigation without broken links.
Output: Placeholder pages with "coming soon" messages.
</objective>

<tasks>

<task type="auto">
  <name>Create placeholder pages</name>
  <files>src/app/(dashboard)/agenda/page.tsx, src/app/(dashboard)/pacientes/page.tsx, src/app/(dashboard)/lista-espera/page.tsx</files>
  <action>
  1. Create src/app/(dashboard)/agenda/page.tsx:
  ```typescript
  import { Calendar } from "lucide-react";
  import { Card, CardContent } from "@/components/ui/card";

  export default function AgendaPage() {
    return (
      <div className="p-6">
        <h1 className="text-2xl font-bold mb-6">Agenda</h1>
        <Card>
          <CardContent className="py-12 text-center">
            <Calendar className="h-16 w-16 mx-auto mb-4 text-gray-300" />
            <h2 className="text-xl font-semibold mb-2">Próximamente</h2>
            <p className="text-gray-500">
              La agenda visual se implementará en la Fase 3.
            </p>
          </CardContent>
        </Card>
      </div>
    );
  }
  ```

  2. Create src/app/(dashboard)/pacientes/page.tsx:
  ```typescript
  import { Users } from "lucide-react";
  import { Card, CardContent } from "@/components/ui/card";

  export default function PacientesPage() {
    return (
      <div className="p-6">
        <h1 className="text-2xl font-bold mb-6">Pacientes</h1>
        <Card>
          <CardContent className="py-12 text-center">
            <Users className="h-16 w-16 mx-auto mb-4 text-gray-300" />
            <h2 className="text-xl font-semibold mb-2">Próximamente</h2>
            <p className="text-gray-500">
              La gestión de pacientes se implementará en la Fase 3.
            </p>
          </CardContent>
        </Card>
      </div>
    );
  }
  ```

  3. Create src/app/(dashboard)/lista-espera/page.tsx:
  ```typescript
  import { Clock } from "lucide-react";
  import { Card, CardContent } from "@/components/ui/card";

  export default function ListaEsperaPage() {
    return (
      <div className="p-6">
        <h1 className="text-2xl font-bold mb-6">Lista de Espera</h1>
        <Card>
          <CardContent className="py-12 text-center">
            <Clock className="h-16 w-16 mx-auto mb-4 text-gray-300" />
            <h2 className="text-xl font-semibold mb-2">Próximamente</h2>
            <p className="text-gray-500">
              La lista de espera se implementará en las Fases 3-4.
            </p>
          </CardContent>
        </Card>
      </div>
    );
  }
  ```
  </action>
  <verify>All pages accessible without errors</verify>
  <done>Placeholder pages ready</done>
</task>

</tasks>

<verification>
- [ ] /agenda shows placeholder
- [ ] /pacientes shows placeholder
- [ ] /lista-espera shows placeholder
- [ ] No broken links in nav
</verification>

<success_criteria>
- All nav links work
- Placeholders inform user
- Clean UI
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-onboarding/01-36-SUMMARY.md`
</output>
