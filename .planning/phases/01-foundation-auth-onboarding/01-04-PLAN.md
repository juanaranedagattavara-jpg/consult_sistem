---
phase: 01-foundation-auth-onboarding
plan: 04
type: execute
wave: 3
depends_on: ["01-01"]
files_modified: [package.json, components.json, tailwind.config.ts, src/lib/utils.ts, src/app/globals.css]
autonomous: true
must_haves:
  truths:
    - "shadcn Button component can be imported"
  artifacts:
    - path: "components.json"
      contains: "shadcn"
    - path: "src/lib/utils.ts"
      exports: ["cn"]
---

<objective>
Setup shadcn/ui with CSS variables and install base components.

Purpose: UI component library for consistent design.
Output: shadcn/ui ready with Button, Input, Card, Form components.
</objective>

<tasks>

<task type="auto">
  <name>Setup shadcn/ui</name>
  <files>components.json, tailwind.config.ts, src/lib/utils.ts, src/app/globals.css</files>
  <action>
  1. npx shadcn@latest init
     - Style: Default
     - Base color: Slate
     - CSS variables: Yes
     - React Server Components: Yes
     - Write to components.json: Yes

  2. Install base components:
     npx shadcn@latest add button input label card

  3. Install additional form dependencies:
     npm install zod react-hook-form @hookform/resolvers

  4. Install other dependencies:
     npm install zustand next-themes lucide-react sonner class-variance-authority clsx tailwind-merge

  5. Verify src/lib/utils.ts has cn() function
  </action>
  <verify>Import { Button } from "@/components/ui/button" works</verify>
  <done>shadcn/ui configured with base components</done>
</task>

</tasks>

<verification>
- [ ] components.json exists
- [ ] Button component in src/components/ui/
- [ ] cn() function in src/lib/utils.ts
- [ ] CSS variables in globals.css
</verification>

<success_criteria>
- shadcn/ui initialized
- Base components installed
- Additional dependencies ready
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-onboarding/01-04-SUMMARY.md`
</output>
