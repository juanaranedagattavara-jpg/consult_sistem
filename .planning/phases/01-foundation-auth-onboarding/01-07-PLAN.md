---
phase: 01-foundation-auth-onboarding
plan: 07
type: execute
wave: 5
depends_on: ["01-06"]
files_modified: [src/lib/validators/auth.ts]
autonomous: true
must_haves:
  truths:
    - "Auth validators export Zod schemas"
  artifacts:
    - path: "src/lib/validators/auth.ts"
      exports: ["loginSchema", "registerSchema"]
---

<objective>
Create Zod validation schemas for authentication forms.

Purpose: Type-safe validation for login and registration.
Output: Reusable Zod schemas for auth forms.
</objective>

<tasks>

<task type="auto">
  <name>Create auth validators</name>
  <files>src/lib/validators/auth.ts</files>
  <action>
  Create src/lib/validators/auth.ts:

  ```typescript
  import { z } from "zod";

  export const loginSchema = z.object({
    email: z.string().email("Email inválido"),
    password: z.string().min(8, "Mínimo 8 caracteres"),
  });

  export const registerSchema = z.object({
    name: z.string().min(2, "Nombre requerido"),
    email: z.string().email("Email inválido"),
    password: z.string().min(8, "Mínimo 8 caracteres"),
    confirmPassword: z.string(),
  }).refine((data) => data.password === data.confirmPassword, {
    message: "Las contraseñas no coinciden",
    path: ["confirmPassword"],
  });

  export type LoginInput = z.infer<typeof loginSchema>;
  export type RegisterInput = z.infer<typeof registerSchema>;
  ```
  </action>
  <verify>TypeScript compiles without errors</verify>
  <done>Auth validators ready</done>
</task>

</tasks>

<verification>
- [ ] File exists at src/lib/validators/auth.ts
- [ ] Exports loginSchema and registerSchema
- [ ] Types exported
</verification>

<success_criteria>
- Zod schemas created
- TypeScript types exported
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-onboarding/01-07-SUMMARY.md`
</output>
