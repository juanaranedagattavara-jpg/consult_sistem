---
phase: 01-foundation-auth-onboarding
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [package.json, convex/_generated/, .env.local]
autonomous: true
must_haves:
  truths:
    - "npx convex dev connects successfully"
  artifacts:
    - path: "convex/_generated/api.d.ts"
      provides: "Convex generated types"
---

<objective>
Install Convex and initialize the development environment.

Purpose: Set up Convex backend connection.
Output: Convex connected and generating types.
</objective>

<tasks>

<task type="auto">
  <name>Install and initialize Convex</name>
  <files>package.json, .env.local</files>
  <action>
  1. npm install convex
  2. npx convex dev --once
     - This will prompt to create a new project or link existing
     - Create new project named "consult-system"
     - This generates .env.local with CONVEX_DEPLOYMENT
  3. Verify convex/_generated/ folder exists
  </action>
  <verify>npx convex dev shows "Convex functions ready"</verify>
  <done>Convex initialized and connected</done>
</task>

</tasks>

<verification>
- [ ] convex package in package.json
- [ ] .env.local has CONVEX_DEPLOYMENT
- [ ] convex/_generated/ exists
</verification>

<success_criteria>
- Convex installed
- Project created in Convex dashboard
- Types generating
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-onboarding/01-02-SUMMARY.md`
</output>
