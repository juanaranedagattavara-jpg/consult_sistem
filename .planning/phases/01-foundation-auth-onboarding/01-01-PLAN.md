---
phase: 01-foundation-auth-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, tsconfig.json, next.config.ts, src/app/page.tsx, src/app/layout.tsx, src/app/globals.css]
autonomous: true
must_haves:
  truths:
    - "npm run dev starts Next.js on localhost:3000"
  artifacts:
    - path: "package.json"
      contains: "next"
    - path: "tsconfig.json"
      contains: "strict"
---

<objective>
Initialize Next.js 15 project with TypeScript strict mode.

Purpose: Foundation for entire application.
Output: Working Next.js app that runs on localhost:3000.
</objective>

<tasks>

<task type="auto">
  <name>Initialize Next.js 15</name>
  <files>package.json, tsconfig.json, next.config.ts</files>
  <action>
  Run: npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --yes

  If directory has files, the command may fail. In that case:
  1. Create package.json manually with next@15, react@19, typescript
  2. Create tsconfig.json with strict: true
  3. Create next.config.ts
  4. Create src/app/page.tsx, layout.tsx, globals.css

  Run npm install after creating files.
  </action>
  <verify>npm run dev starts without errors</verify>
  <done>Next.js running on localhost:3000</done>
</task>

</tasks>

<verification>
- [ ] npm run dev works
- [ ] localhost:3000 shows Next.js page
</verification>

<success_criteria>
- Next.js 15 project initialized
- TypeScript strict mode enabled
- App runs successfully
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-onboarding/01-01-SUMMARY.md`
</output>
