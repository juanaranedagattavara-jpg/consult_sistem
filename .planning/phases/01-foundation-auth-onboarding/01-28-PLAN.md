---
phase: 01-foundation-auth-onboarding
plan: 28
type: execute
wave: 13
depends_on: ["01-23", "01-24", "01-25", "01-26", "01-27"]
files_modified: [src/app/(dashboard)/settings/clinic/page.tsx, src/app/(dashboard)/settings/appearance/page.tsx, src/app/(dashboard)/settings/profile/page.tsx]
autonomous: true
must_haves:
  truths:
    - "Settings pages accessible at /settings/*"
  artifacts:
    - path: "src/app/(dashboard)/settings/clinic/page.tsx"
      min_lines: 30
    - path: "src/app/(dashboard)/settings/appearance/page.tsx"
      min_lines: 20
    - path: "src/app/(dashboard)/settings/profile/page.tsx"
      min_lines: 20
---

<objective>
Create settings pages for clinic, appearance, and profile.

Purpose: Settings accessible from dashboard.
Output: Working settings pages with tabs and forms.
</objective>

<tasks>

<task type="auto">
  <name>Create settings pages</name>
  <files>src/app/(dashboard)/settings/clinic/page.tsx, src/app/(dashboard)/settings/appearance/page.tsx, src/app/(dashboard)/settings/profile/page.tsx</files>
  <action>
  1. Install tabs component: npx shadcn@latest add tabs

  2. Create src/app/(dashboard)/settings/clinic/page.tsx:
  ```typescript
  "use client";

  import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
  import { ClinicForm } from "@/components/settings/clinic-form";
  import { BotConfigForm } from "@/components/settings/bot-config-form";
  import { TimingForm } from "@/components/settings/timing-form";

  export default function ClinicSettingsPage() {
    return (
      <div className="p-6">
        <h1 className="text-2xl font-bold mb-6">Configuración de la clínica</h1>

        <Tabs defaultValue="general" className="w-full">
          <TabsList className="mb-4">
            <TabsTrigger value="general">General</TabsTrigger>
            <TabsTrigger value="bot">Bot WhatsApp</TabsTrigger>
            <TabsTrigger value="timing">Tiempos</TabsTrigger>
          </TabsList>

          <TabsContent value="general">
            <ClinicForm />
          </TabsContent>

          <TabsContent value="bot">
            <BotConfigForm />
          </TabsContent>

          <TabsContent value="timing">
            <TimingForm />
          </TabsContent>
        </Tabs>
      </div>
    );
  }
  ```

  3. Create src/app/(dashboard)/settings/appearance/page.tsx:
  ```typescript
  import { AppearanceForm } from "@/components/settings/appearance-form";

  export default function AppearanceSettingsPage() {
    return (
      <div className="p-6">
        <h1 className="text-2xl font-bold mb-6">Apariencia</h1>
        <AppearanceForm />
      </div>
    );
  }
  ```

  4. Create src/app/(dashboard)/settings/profile/page.tsx:
  ```typescript
  import { ProfileForm } from "@/components/settings/profile-form";

  export default function ProfileSettingsPage() {
    return (
      <div className="p-6">
        <h1 className="text-2xl font-bold mb-6">Mi perfil</h1>
        <ProfileForm />
      </div>
    );
  }
  ```
  </action>
  <verify>Pages accessible at /settings/clinic, /settings/appearance, /settings/profile</verify>
  <done>Settings pages ready</done>
</task>

</tasks>

<verification>
- [ ] /settings/clinic shows tabs
- [ ] /settings/appearance shows form
- [ ] /settings/profile shows form
- [ ] All forms functional
</verification>

<success_criteria>
- All settings pages working
- Tabs navigate correctly
- Forms save data
</success_criteria>

<output>
Create `.planning/phases/01-foundation-auth-onboarding/01-28-SUMMARY.md`
</output>
